{
  "homepage": "https://haris-r.github.io/Portfolio",
  "name": "portfolio_nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider next build",
    "start": "next start",
    "export": "next export",
    "predeploy": "npm run build && npm run export",
    "deploy": "gh-pages -d out --dotfiles"
  },
  "dependencies": {
    "next": "10.2.3",
    "nvm": "^0.0.4",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-icons": "^4.2.0",
    "styled-components": "^5.3.0",
    "styled-normalize": "^8.0.7"
  },
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "cross-env": "^7.0.3"
  }
}
